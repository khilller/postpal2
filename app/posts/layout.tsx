import React from "react";

export const metadata = {
  title: "Posts List | Blogify",
  description: "Create social media posts with just one click",
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
